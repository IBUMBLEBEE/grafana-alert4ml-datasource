apiVersion: 1

# Intentionally empty — provisioning custom plugin datasources at startup can race
# plugin registration and crash Grafana. Copy ../datasources.example.yml to
# datasources.yml for local dev, or let E2E tests create datasources via createDataSource().
datasources: []
